version: '3.8'
services:
  encoders_gag:
    build: .
    ports:
      - "8080:8080"
    environment:
      - ENCODERS_GAG_PORT=8080
      - ENCODERS_GAG_HOST=0.0.0.0
    volumes:
      - ./data:/data
